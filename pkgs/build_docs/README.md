# 개요

This package checks for forbidden Moonwave tags and builds the documentation.

## Usage

Run the script from the repository root.

```sh
lune run pkgs/build_docs/src/init.luau
```

## Requirements

- `npx` must be available in PATH.
- The build runs `moonwave` via `npx`.
